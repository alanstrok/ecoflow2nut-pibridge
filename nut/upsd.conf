# upsd listens for NUT clients on all interfaces, port 4141 (the default 3493
# is already in use on the test host). Restrict LISTEN to a specific
# address/subnet in production if desired.

LISTEN 0.0.0.0 4141
LISTEN :: 4141

MAXAGE 30
