# NUT operating mode.
#
# "netserver" runs the dummy-ups driver plus upsd to serve NUT clients over the
# network. Without this, upsd refuses to start ("upsd disabled, please adjust
# the configuration").
MODE=netserver
